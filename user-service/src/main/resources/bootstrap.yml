spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://config-server:9998


security:
  user:
    name: liuwei
    password: liuwei123

eureka:
  instance:
    hostname: user-service
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://${security.user.name}:${security.user.password}@eureka-server:9999/eureka/